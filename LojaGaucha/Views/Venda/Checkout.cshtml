@model IEnumerable<LojaGaucha.Models.Venda>

@{
    ViewBag.Title = "Checkout";
}

<h2>Checkout</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Pessoa.PessoaNome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CarrinhoId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Pessoa.PessoaNome)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CarrinhoId)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.VendaId }) |
            @Html.ActionLink("Details", "Details", new { id=item.VendaId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.VendaId })
        </td>
    </tr>
}

</table>
